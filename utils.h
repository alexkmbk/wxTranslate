#pragma once
#include <time.h>
#include <ctime>
#include <stdio.h>
#include <string>
#include <chrono>
#include <algorithm>
#include <vector>
#include <filesystem>
using namespace std;

std::vector<std::string> split(const std::string &s, char delim);
